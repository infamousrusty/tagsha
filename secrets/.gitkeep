# This directory holds Docker secrets at runtime.
# Files in this directory are excluded from git via .gitignore.
# Create files here for local development only:
#   echo 'ghp_your_token' > secrets/github_token
#   echo 'strongpassword' > secrets/grafana_admin_password
